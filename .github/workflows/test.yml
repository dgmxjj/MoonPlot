name: Check and Test

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

env:
  # Keep CI on the current official distribution; the project remains
  # compatible with the committee's MoonBit 0.10.3 acceptance baseline.
  MOONBIT_INSTALL_VERSION: "latest"

jobs:
  build:
    name: MoonBit (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc libssl-dev

      - name: Install macOS dependencies
        if: matrix.os == 'macos-latest'
        run: brew install gcc openssl

      - name: Install MoonBit on Unix
        if: matrix.os != 'windows-latest'
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Install MoonBit on Windows
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
          irm https://cli.moonbitlang.com/install/powershell.ps1 | iex
          "C:\Users\runneradmin\.moon\bin" | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Install Windows native dependencies
        if: matrix.os == 'windows-latest'
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-openssl

      - name: Toolchain information
        run: |
          moon version --all
          moon update

      - name: Check all targets
        run: moon check --target all --deny-warn

      - name: Build all targets
        run: moon build --target all --deny-warn

      - name: Test all targets
        run: moon test --target all --deny-warn

      - name: Run benchmark example
        run: |
          moon run examples/benchmark_summary --target wasm > benchmark.svg
          grep -q 'UCI Iris petal width (n=18)' benchmark.svg
          grep -q '<line ' benchmark.svg
          rm -f benchmark.svg

      - name: Check formatting
        run: moon fmt --check src examples

      - name: Check generated public interfaces
        run: |
          moon info
          find . -name 'pkg.generated.mbti' -type f -print0 | xargs -0 -r sed -i '${/^$/d;}'
          git diff --exit-code

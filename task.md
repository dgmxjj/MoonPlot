- [x] 1. 基础配置与构建 file 修改
    - [x] 修改 `moon.mod.json`，将版本升级至 `0.2.0`
    - [x] 修改 `.gitignore`，忽略本地构建产物 `_build/`
    - [x] 编写 `.github/workflows/ci.yml`，补齐 `check/fmt/info/test` CI 检查
- [x] 2. 比例尺 (Scales) 完善
    - [x] 配置 `src/coord/moon.pkg.json`，导入 `"moonbitlang/core/math"`
    - [x] 在 `src/coord/scale.mbt` 中实现 `LogarithmicScale` 比例尺
    - [x] 在 `src/coord/scale.mbt` 中实现 `CategoryScale` 比例尺
    - [x] 修复 `scale.mbt` 中的 warnings，确保其实现通用 ContinuousScale 或直接导出
- [x] 3. Canvas 渲染后端实现
    - [x] 创建新文件 `src/backend/canvas.mbt`
    - [x] 实现 `CanvasBackend` 结构体及 `Backend` 接口
- [x] 4. 布局引擎、图例与防冲突网格 (Layout, Legend, Axis)
    - [x] 重构 `src/chart/chart.mbt`：实现自适应布局 (Adaptive Layout)、图例 (Legend) 以及动态计算网格和刻度防碰撞 (Tick Anti-collision)
    - [x] 修改 `src/chart/axis.mbt`：支持刻度防碰撞，支持 X 轴离散分类标签渲染
- [x] 5. 系列绘制逻辑 (Series)
    - [x] 修改 `src/series/line.mbt`：适配更新后的比例尺/布局
    - [x] 修改 `src/series/scatter.mbt`：适配更新后的比例尺/布局
    - [x] 修改 `src/series/bar.mbt`：支持离散柱状图绘制 (CategoryScale)
- [x] 6. 示例与测试验证
    - [x] 编写 `src/chart/svg_test.mbt`，添加 SVG 渲染输出校验测试
    - [x] 更新 `examples/basic_line/main.mbt`，真正绘制折线图并输出保存为 `line_chart.svg`
    - [x] 新建 `examples/basic_bar`，展示柱状图、分类/对数比例尺等综合示例，输出 `bar_chart.svg`
    - [x] 运行 `moon check`, `moon fmt`, `moon info`, `moon test` 确保完全通过
- [x] 7. 发布与推送
    - [x] 执行 `moon publish` 发布最新版到 mooncakes.io
    - [x] 推送最新修改到 GitHub 仓库 `origin`
    - [x] 推送最新修改到 GitLink 仓库 `gitlink` (使用提供密码登录 `Dgmxjj`/`Ddd123489`)
